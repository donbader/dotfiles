# ---------------------
# Autojump Configuration (macOS)
# ---------------------
# Smart directory navigation

[ -f $HOMEBREW_PREFIX/etc/profile.d/autojump.sh ] && . $HOMEBREW_PREFIX/etc/profile.d/autojump.sh
